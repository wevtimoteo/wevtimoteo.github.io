<h1><%= @page.title %></h1>

<em>Posted on <%= @page.date %></em>

<%= @contents %>

<%= unless Enum.empty?(@page.tags) do %>
  <h3>Tags</h3>

  <ul>
    <%= for t <- @page.tags do %>
      <li><a href="<%= t.list_url %>"><%= t.name %></a></li>
    <% end %>
  </ul>
<% end %>
